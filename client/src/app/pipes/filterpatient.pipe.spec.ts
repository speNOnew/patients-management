import { FilterpatientPipe } from './filterpatient.pipe';

describe('FilterpatientPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpatientPipe();
    expect(pipe).toBeTruthy();
  });
});
